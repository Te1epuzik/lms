import { Metadata } from "next"

export const metadata: Metadata = {
	title: "sign up"
}

export default function SignUp() {
	return (
		<div style={{
			height: "400px"
		}}>
			<h2>Sign UP page</h2>
		</div>
	);
}